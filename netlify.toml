[build]
  command = "npm install && npm run build --workspace=apps/tennis-web-v3"
  publish = "apps/tennis-web-v3/dist"

# SPA client-side routing fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
